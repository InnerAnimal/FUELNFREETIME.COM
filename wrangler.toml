name = "fuelnfreetime"
compatibility_date = "2025-11-17"
compatibility_flags = ["nodejs_compat"]

workers_dev = true

# Static assets — public/ only (excludes node_modules)
[assets]
directory = "./public"
html_handling = "auto-trailing-slash"
not_found_handling = "404-page"

[vars]
ENVIRONMENT = "production"
